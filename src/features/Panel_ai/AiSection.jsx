// import { getResponded } from '../../apis/api/feedback';

const AiSection = () => {
  return (
    <>
      <div>AI 섹션</div>
      {/* <button
        onClick={async () => {
          const data = await getResponded('홍릉두물길');
        }}>
        응답된거
      </button> */}
    </>
  );
};

export default AiSection;
